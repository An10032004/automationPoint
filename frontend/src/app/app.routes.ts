import { Routes } from '@angular/router';
import { TestComponent } from './features/test/test';
import { EssayComponent } from './features/essay/essay';

export const routes: Routes = [
    {
        path: '',
        component: EssayComponent
    },
];  
